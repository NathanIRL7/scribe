name: Weekly Market Research

on:
  schedule:
    - cron: '0 8 * * 1'  # Jeden Montag 8 Uhr
  workflow_dispatch:  # Auch manuell startbar

jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - name: Run Market Research
        run: echo "Agent läuft"
